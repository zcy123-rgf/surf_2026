# 组会四个问题：当前证据、实验设计与判定标准

本文只记录可追溯的结果和下一次独立实验，不把拟合一致性写成检测准确率。

## 已核验的 20 帧结果

- 数据：KITTI Odometry Sequence 00，帧 `000000-000019`，统一到帧 19；输入审计文件 SHA256 为 `1694ab2bb387fe0d42dc271f7122827ee13639227dd915d907930e4ac3fad324`。
- 左右各有 1180 个 CLRNet 派生、IPM 投影并经 pose 对齐的点。
- 留一帧验证：左侧 RMSE 0.1608 m、P95 0.3404 m；右侧 RMSE 0.1341 m、P95 0.2734 m。
- 两条拟合曲线不相交；两线中位间距为 7.0527 m。前 5 帧实验的中位间距为 7.2361 m，所以 20 帧保持了原先“按底部横坐标选择最外侧两条候选线”的语义。它更接近道路两侧边界，不应在未检查图片时改称“自车单车道边界”。
- 每个 0.5 m 区间的支持帧数中位数是 5，但最小值是 1。远端或尾部只有单帧支持的区间可信度较低，应与核心重叠区分开报告。
- 以上误差只表示曲线能够解释检测点；它不证明 CLRNet 选线正确，也不是真值误差。

上传的同一份 20 帧点集已完成分段压缩实算：2360 个原始点变为 64 个短段特征点，点数压缩 36.875 倍。共同覆盖范围内，分段特征点再融合相对直接拟合的对称 Chamfer 为左 0.0618 m、右 0.0617 m，P95 为左 0.1384 m、右 0.2003 m，最大距离为左 0.2130 m、右 0.2542 m。完整范围最大距离较大（左 0.8520 m、右 0.3952 m）主要来自二次融合曲线端点覆盖范围略长，故必须与共同范围形状误差分开报告。

近直路模型比较中，二次多项式、三次多项式和 B 样条的平均留一帧 RMSE 分别为 0.12787 m、0.12745 m 和 0.12690 m；差异远小于各自约 0.0127-0.0128 m 的标准误。因此 0-19 帧没有证据表明更复杂模型显著优于二次多项式，B 样条的价值需要在弯道候选上验证。

相对旧的前五帧人工伪真值，直接 20 帧曲线的对称 Chamfer 为左 1.1966 m、右 1.2668 m，F1@0.5 m 只有左 0.2138、右 0.1824。旧五帧实验本身也已有约 1.28-1.38 m 的同类差异，且标注说明明确写有“视觉道路边界、遮挡处人工插值”。所以这不是压缩造成的新退化，而是当前人工标注与 CLRNet 最外侧候选线的目标语义/位置并不一致；该标注只能作为诊断性伪参考，不能包装成可靠准确率真值。

## 问题 1：连续帧怎样拟合成左右两条平滑曲线

当前可复现实验方案是：保持左右身份分开；所有点用 `inv(T_world_ref) @ T_world_src` 对齐；按 0.5 m 区间让每帧最多贡献一次中位数；分别用 Huber-IRLS 鲁棒三次参数 B 样条拟合；用留一帧误差的一标准误规则选择平滑参数；禁止超出观测范围外推。

判定分两层：先检查原图、全部候选线和被选两线，确认车道身份连续；再检查不相交、支持帧数、LOFO RMSE/P95。几何指标不能替代第一层。

## 问题 2：弯道和更多帧时模型是否合适

0-19 帧航向变化只有约 2.18°，只能验证近直路。新脚本会实际比较鲁棒 1/2/3 次 `x(z)` 多项式和当前三次参数 B 样条的留一帧误差。多项式要求同一个 `z` 只有一个 `x`；急弯、回转或参考坐标不合适时，这一条件会失败。

当前 B 样条虽然是参数曲线，但预处理仍按参考帧 `z` 排序，因此也不能仅凭“B 样条”三个字宣称已适用于所有弯道。脚本选择两个轻量候选区间：`0095-0114`（pose 航向变化约 +56.5°）和 `1549-1568`（约 -59.2°）。这两个区间来自先前 SemanticKITTI class 60 点较连续的范围；class 60 只说明存在车道标记点，不保证是连续左右边界。只有联系表确认道路拓扑和 CLRNet 选线连续后，弯道误差才有解释价值。若 `z` 排序失效，下一步改成沿车辆轨迹的 Frenet/弧长顺序，而不是继续提高多项式次数。

## 问题 3：短路段怎样压缩并继续融合

轻量实验把 20 帧分为 4 个连续五帧段。每段分别拟合左右曲线，再对每条曲线按弧长均匀抽取 8 个特征点；所有短段已经处于同一 pose 参考坐标，因此可把这些等数量特征点再次鲁棒拟合成全局左右曲线。

输出同时记录原始点数、短段 B 样条控制系数数目、导出的特征点数、压缩比，以及“分段特征点再融合曲线”相对“20 帧直接拟合曲线”的对称 Chamfer、P95 和最大距离。最后三项是压缩保真度，不是真值准确率。

## 问题 4：怎样替换像素重叠率

人工标注的前 5 帧被明确称为“人工伪真值”，不称 KITTI 官方真值，也不参与参数选择。评价仅在预测与人工曲线共同覆盖的米制 BEV 范围内进行：

- 对称 Chamfer 均值：预测曲线到人工点、人工点到预测曲线两个方向的平均距离；
- 对称 P95：比最大距离稳定，同时能反映较差的 5% 区域；
- Precision、Recall、F1@0.2/0.3/0.5 m：用米制距离阈值表示预测是否靠近人工标注；
- 覆盖范围和左右分别报告，避免一侧好结果掩盖另一侧。

这些指标直接评价几何位置，比栅格像素重叠率更接近“车道曲线是否落在正确位置”。但它们的上限仍受人工标注质量、IPM 平路假设和 pose 误差影响。

## 运行方式

在工作站的 Anaconda PowerShell Prompt 中运行：

```powershell
Set-Location F:\2026_surf
git switch agent/windows-ransac-workstation-data
git pull --ff-only origin agent/windows-ransac-workstation-data

.\scripts\run_meeting_four_questions_windows.ps1 `
  -EnvName "surf2026-win" `
  -DatasetRoot "F:\BaiduNetdiskDownload\kitti\odometry" `
  -Device cuda
```

脚本只读取最新的 `first20_two_curves_*`，把所有新结果写入独立的 `workstation_outputs\meeting_four_questions_时间戳`，并拒绝复用非空输出目录。某个弯道候选失败时会记录失败原因并继续另一个候选，不会伪造缺失结果。

## 方法依据

- [KITTI 官方 Odometry 页面](https://www.cvlibs.net/datasets/kitti/eval_odometry.php)：Sequence 00-10 提供轨迹，pose 用于帧间统一坐标；该页面没有提供逐帧左右车道曲线真值。
- [Feng 等，CVPR 2022，Rethinking Efficient Lane Detection via Curve Modeling](https://openaccess.thecvf.com/content/CVPR2022/html/Feng_Rethinking_Efficient_Lane_Detection_via_Curve_Modeling_CVPR_2022_paper.html)：讨论多项式车道表示的困难，并用少量参数的三次 Bézier 曲线表示完整车道形状，支持本项目采用紧凑参数曲线而不是保存所有稠密点。
- [HDMapNet，ICRA 2022](https://arxiv.org/abs/2107.06307)：以 BEV 中的矢量化地图元素为对象，并区分语义级和实例级评价，支持本项目不再只看栅格重叠。
- [MapTR，ICLR 2023](https://arxiv.org/abs/2208.14437)：把地图元素建模为点集并处理等价排列，支持使用米制矢量曲线/点集距离检查地图几何，而非只比较渲染像素。
