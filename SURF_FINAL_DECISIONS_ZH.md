# SURF 最终范围、方法与结论

## 一、正式实验范围

- 正式结果只使用 **KITTI Odometry Sequence 01，帧 851–1005**，共 155 帧。
- 已人工核验的直道种子为 **851–875** 和 **991–1005**。
- Sequence 03 帧 29–128 与 Sequence 07 帧 415–514 不再进入正式实验、汇总指标或最终结论。
- 所有几何保存为同一参考相机下的米制笛卡尔 `X/Z`；没有使用 Frenet 坐标。

## 二、组会后八点的最终处理

### 1. 用位姿曲率区分直道、过渡段和弯道

程序从 KITTI pose 的平移轨迹取得 `X/Z`，按路程均匀重采样后，用 15 m Savitzky–Golay 局部多项式平滑并求一、二阶导数。曲率为：

`kappa = (x' z'' - z' x'') / (x'^2 + z'^2)^(3/2)`，单位为 `1/m`。

Sequence 01 的正式阈值由已核验直道种子的噪声标定：

- 直道基线中位数：`0.0008879907 1/m`；
- 稳健标准差：`0.0004052101 1/m`；
- 直道阈值：`max(0.0015, 中位数 + 3×稳健标准差) = 0.0021036211 1/m`；
- 弯道阈值：`max(0.0030, 中位数 + 6×稳健标准差, 1.5×直道阈值) = 0.0033192514 1/m`；
- `|kappa|` 不高于直道阈值为直道，不低于弯道阈值为弯道，中间为进入/驶出过渡；状态切换至少连续 5 帧才确认。

这两个数是可复现的**项目阈值**，不是 KITTI 官方标签，也不是适用于所有道路的论文通用常数。论文支持的是依据曲率区分直线、圆曲线和过渡曲线的几何原则；具体数值仍应由测量噪声和目标路段标定。

### 2. 直道多项式、弯道模型比较和窗口接口

- 15 帧窗口、步长 10 帧，相邻窗口重叠 5 帧；左右车道分别处理。
- 直道窗口固定使用二次参数多项式。
- 过渡和弯道窗口在相同输入上比较二次参数多项式与三次参数 B 样条。
- 比较指标为整帧留出后的 RMSE；当差异落在预先登记的简化容差内时优先多项式，否则采用误差更小的模型。
- 相邻窗口只有在重叠区 P95 距离和切向夹角均通过门限时才融合；失败就断开，不强画连续线。

Sequence 01 的已核验结果：30 个可能的“窗口×车道侧”中完成 28 个；直道选择多项式 4 次，过渡选择 B 样条 4 次，弯道选择 B 样条 18 次、选择多项式 2 次；26 个接口中 25 个通过连续性检查。该结果支持“直道固定多项式、弯道按留出误差选择模型”，而不是预先宣称所有弯道都必须使用 B 样条。

### 3. 遮挡和车道身份

正式结果不再使用 Sequence 03/07，因此不把严重遮挡补全作为核心成果。当前跨帧左右编号是本项目的关联结果，不是 CLRNet 固定语义车道 ID；`temporal_ego` 已被否定，不再作为最终方案。

若 Sequence 01 中出现短缺口，只允许输出虚线的低置信度三次 Hermite 几何假设，并同时保存端点距离、切向夹角、最大曲率和置信度。它不是检测观测，也不能用于宣称真实车道位置。

### 4. 多帧融合的最终输出

每帧 CLRNet 采样点先经 IPM 变成米制地面 `X/Z` 点，再用 KITTI pose 变换到公共参考帧。窗口内鲁棒聚合并拟合左右曲线；重叠窗口通过接口检查后加权融合。最终输出包括观测曲线节点、分段编号、模型选择、接口诊断和可选虚线假设。

### 5. 工作站最终复现与目录

- 最终工作区：`F:\surf_final`。
- 正式一键入口：`scripts\run_surf_final_sequence01_windows.ps1`。
- 每次运行自动建立新的时间戳目录，拒绝覆盖非空输出；旧实验不会混入新结果。
- 可使用 `-RunFullPoseAudit` 先重新执行 Sequence 00–10 的 pose-only 候选扫描，但正式结果仍只来自 Sequence 01。

### 6. 没有官方车道真值时的评估

不再使用像素重叠率作为主要指标，也不把内部误差称作真实准确率。最终报告包含：

1. 观测覆盖：左右各自有效帧、双侧同时有效帧、最长缺失；
2. 拟合覆盖：成功窗口侧数量/理论数量；
3. 整帧留出一致性：Mean、RMSE、Median、P90、P95、Max；
4. 接口安全性：P95 距离、切向夹角、通过率、断点；
5. 假设桥审计：端点距离、切向夹角、最大桥曲率、置信度。

这些指标比较的是方法对 CLRNet/IPM 观测的稳定性、覆盖和几何一致性，不是现实世界车道定位精度。

### 7. SLAM 范围

本阶段使用 KITTI 已发布 pose 做多帧对齐，没有实现或评测一个新的 SLAM 后端。最终表述为“pose-aligned multi-frame lane fusion prototype”，不写成“完成了完整 SLAM 系统”。

### 8. 4D 语义地图范围

本阶段没有完成动态目标时序语义或完整 4D 地图。交付物是车道检测驱动的、带时间顺序输入和位姿对齐的长路段车道几何原型。该限制必须在海报和汇报中明确。

## 三、依据

- Garach, de Oña, Pasadas, *Automation in Construction*, 2014：道路平面线形可按曲率区分直线、圆曲线和过渡曲线；数值阈值依赖数据与道路类型。https://doi.org/10.1016/j.autcon.2014.07.002
- Savitzky & Golay, *Analytical Chemistry*, 1964：用局部最小二乘多项式进行平滑和数值微分。https://doi.org/10.1021/ac60214a047
- Liu et al., *Journal of Computational Design and Engineering*, 2015：相邻直线/B 样条或 B 样条段连接时，需要端点位置与切向连续性约束。https://academic.oup.com/jcde/article/2/4/218/5715267
- StreamMapNet, WACV 2024：历史地图信息经位姿补偿后用于时序建图；本项目只借鉴“位姿补偿历史几何”的思想，没有复现或训练该模型。https://openaccess.thecvf.com/content/WACV2024/html/Yuan_StreamMapNet_Streaming_Mapping_Network_for_Vectorized_Online_HD_Map_Construction_WACV_2024_paper.html
